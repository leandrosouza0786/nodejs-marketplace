<head>
  <style>
    body {
      font-family: Arial, Helvetica, sans-serif
    }
  </style>
</head>

<body>
  <strong>Olá, {{ ad.author.name}} </strong>
  <ap> Você tem uma nova solitação de compra para o anúncio {{ ad.title}}</ap>
  <br>
  <strong> {{ user.name }} ({{ user.email }}):</strong>
  <p>{{content}}</p>
</body>

</html>
